apiVersion: v1
kind: Service
metadata:
  name: gitops-demo-svc
spec:
  selector:
    app: gitops-demo
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
